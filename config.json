{
    "_what":"The disablers proxy."
    "webserver": "WEBSERVER_FLAG"
    "ports":{
        "arch":"ARCH_PROXY_PORT, // port for the proxy server"
       "web":"WEB_UI_PORT"
    },
    "fill-list":"{ // address arch listens on. if you leave it on 0.0.0.0 you can via all IPs"
    	"your_IP": "0.0.0.0"_required, trust us_,
	"web": "0.0.0.0"

}
